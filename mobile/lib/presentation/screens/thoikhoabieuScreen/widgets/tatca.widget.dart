import 'package:flutter/material.dart';

class TatCa extends StatelessWidget {
  const TatCa({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tất cả"),
    );
  }
}
