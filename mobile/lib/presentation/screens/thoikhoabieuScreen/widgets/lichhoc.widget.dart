import 'package:flutter/material.dart';

class LichHoc extends StatelessWidget {
  const LichHoc({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Lịch học"),
    );
  }
}
